<nav class="navbar navbar-expand-lg navbar-light bg-light">
	<a class="navbar-brand" href="/">It's My practice app</a>

    <div class="collapse navbar-collapse" id="navbarSupportedContent">
      
    </div>

    <ul class="navbar-nav mr-auto">
        <% if signed_in? %>
          <a class="navbar-text">Welcome <%= current_user.email %></a>
          <li class="nav-item"><%= link_to "Sign out", destroy_user_session_path, method: :delete, class: 'nav-link' %></li>
        <% else %>
          <li class="nav-item">
          	<%= link_to "Registration", new_user_registration_path, class: 'nav-link' %>
          </li>
          <li class="nav-item">
          	<%= link_to "Sign in", user_session_path, class: 'nav-link' %>
          </li>
        <% end %>
      </ul>
</nav>
